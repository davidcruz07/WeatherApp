import { KelvinCelsiusPipe } from './kelvin-celsius-pipe';

describe('KelvinCelsiusPipe', () => {
  it('create an instance', () => {
    const pipe = new KelvinCelsiusPipe();
    expect(pipe).toBeTruthy();
  });
});
